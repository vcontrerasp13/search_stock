import { pool } from '@/lib/db';

export const searchedModel = {
    get: async () => { },
    insert: async (id_user_establec, description) => {
        try {

        } catch (error) {

        }
    }
}